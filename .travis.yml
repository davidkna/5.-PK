language: c
sudo: required
install:
    - sudo apt-get install build-essential
    - sudo add-apt-repository ppa:ubuntu-toolchain-r/test
    - sudo apt-get update
    - sudo apt-get install gcc-5.1 g++-5.1 cpp-5.1
    - cd /usr/bin
    - sudo rm gcc g++ cpp
    - sudo ln -s gcc-5.1 gcc
    - sudo ln -s g++-5.1 g++
    - sudo ln -s cpp-5.1 cpp
script:
    - cd ./PK/build/
    - ./build.sh
    - cd ../..
    - cd ./main/build/
    - ./build.sh
